import 'package:flutter/material.dart';

class DynamicUpdate extends StatelessWidget {
  const DynamicUpdate({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.red,
    );
  }
}
