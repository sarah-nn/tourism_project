import 'package:flutter/material.dart';

class CheckStaticBookWidget extends StatelessWidget {
  const CheckStaticBookWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Dialog(
      child: Text("data"),
    );
  }
}
